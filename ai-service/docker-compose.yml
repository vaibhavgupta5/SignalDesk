version: '3.8'
services:
  ai-api:
    build: .
    container_name: signaldesk-ai-local
    restart: unless-stopped
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
